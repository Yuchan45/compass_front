interface:
  display_name: "Compass Commit"
  short_description: "Prepare safe atomic frontend commits"
  default_prompt: "Use $compass-commit to prepare a safe atomic commit for the current Compass frontend changes."
